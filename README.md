# Quick Start

Install dependencies:

    $ npm install -d

Start the server:

    $ node app.js

# Heroku

    $ heroku create -s cedar
    $ heroku config:add NODE_ENV=production
    $ heroku create -s cedar